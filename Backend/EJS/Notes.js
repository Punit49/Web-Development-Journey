/* ============================================================
   📌 BACKEND — INTRODUCTION TO EJS
   ============================================================

   🔹 Templating -
   It is the process of generating dynamic layouts(web pages), Templates 
   are files that generate dynamic html using data coming from server, 
   they act as a blueprint for creating multiple same type of web pages. 

   ------------------------------------------------------------

   🔹 EJS - [Embedded JavaScript] -
   It is a templating engine used to create
   dynamic web pages, it lets you embed js directly in html using tags, rendering 
   data from server to client's browser. express automatically require it.

   ------------------------------------------------------------

   🔹 app.set() -
   It is used to store configuration settings, like view engine, views directory, 
   enviornment variables and custom properties.

   ------------------------------------------------------------

   🔹 view engine -
   A view engine is tool used to render dynamic html pages, it converts 
   templates + data (from server) to final html page, before sending it to client. 
   view engine accepts all our templates to be in views folder.

   ------------------------------------------------------------

   🔹 Path module in node.js -
   It helps work with file and directory paths, 
   Makes paths OS-independent.

   ------------------------------------------------------------

   🔹 __dirname -
   It is global variable in node.js, gives the absolute path of current directory.

   ------------------------------------------------------------

   🔹 Interpolation syntax -
   It allows javascript expressions inside html using EJS special tags to dynamically
   render content. Ex - <%= %>

   ============================================================
*/
