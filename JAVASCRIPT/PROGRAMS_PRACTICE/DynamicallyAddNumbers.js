// ? Program 1 - Dynamically Add Numbers.
    // let a = prompt("Enter First Number: ", "");
    // let b = prompt("Enter Second Number: ", "");

    // console.log("Sum Is - ", Number(a) + Number(b)); // Try to convert Whole String Number, NaN if any part of the string is non-numeric.
    // console.log("Sum Is: ",parseInt(a) + parseInt(b)); // Convert to num until encounters non-digit num. 

//  ? Program 2 - Square Root of a number.
    let num = parseInt(prompt("Enter Number - ", ""));
    document.write(num * num);
    // document.write(Math.sqrt(num))

//  ? 
//  ? 
//  ? 
//  ? 
//  ? 
//  ? 
//  ? 
//  ? 
//  ? 
//  ? 