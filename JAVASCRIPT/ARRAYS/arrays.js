// ! Array - An array in JavaScript is a data structure used to store multiple values in a single variable. It can hold various data types and allows for dynamic resizing. 

// * WAYS TO MAKE AN ARRAY - 
    // ? 1. USING ARRAY LITERALS [] -
        let MyArray = [10,20,30,40,50];
        let myskills = ["html", "css", "javascript", "react"];

    // ? 2. USING ARRAY CONSTRUCTOR - Creating an Array using Array Constructor (JavaScript new Keyword). The “Array Constructor” refers to a method of creating arrays by invoking the Array constructor function. This approach allows for dynamic initialization and can be used to create arrays with a specified length or elements.

        // CREATING AN EMPTY ARRAY WITH SPECIFIED LENGTH - 
        // let emptyArr = new Array(20);
        // console.log(emptyArr);
        
        // let MyNewArray = new Array(70, 20, 50);
        // MyNewArray[0] = 10;
        // console.log(MyNewArray);

// ! NOTE - 
// ? 1. If we try to access an index of array which is not exists, then output will be undefined.
