// ? WHAT IS WINDOW OBJECT - Created By browser, All things lies under this.
// *   WINDOW OBJECT is the global object in the browser environment. 
// *   It represents the browser's window or frame and provides an interface for 
// *   accessing and manipulating the browser window and the DOM (Document Object Model). 
// *   The window object contains many properties and methods that allow you to interact with the browser and the web page.

// * 1. Global Scope: The window object is the global object in the browser environment. 
// *    This means that variables and functions declared globally are properties and methods of the window object.

