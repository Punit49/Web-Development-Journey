// ! MATH -

    // * 1. PI
    // console.log(Math.PI);

    // * 2. Maximum
    // console.log(Math.max(100,300,500));

    // * 3. Minimum
    // console.log(Math.min(100,300,500));

    // * 4. return smallest number near to given numeric value
    // console.log(Math.floor(1.9));

    // * 5. return greatest number near to given numeric value
    // console.log(Math.ceil(1.1));

    // * 6. roundof number
    // console.log(Math.round(1.8));

    // * 7. converts negative number into positive.
    // console.log(Math.abs(-10));

    // * 8. returns random number between 0 and 1[exclusive].
    // console.log(Math.random());

     // * 9. Pow
    //  console.log(Math.pow(10,2));

     // * 10. returns square root
    //  console.log(Math.ceil(1.1));



    




    
